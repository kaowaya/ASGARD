---
name: "DQN深度Q网络"
description: "Deep Q-Network，Q-learning与神经网络的结合，用于离散动作空间控制"
metadata:
  skill_id: "F0.31"
  category: "强化学习类"
  subcategory: "Value-based"
  tags: ["DQN", "Q-learning", "深度强化学习", "离散动作"]
  battery_types: ["LFP", "NCM", "Na-ion", "半固态", "全固态"]
  deployment: ["云端训练", "边缘推理"]
  input_format: "状态s"
  output_format: "动作价值Q(s,a)"
  complexity: "高"
  universality: "100%"
---

# F0.31 DQN深度Q网络

## 算法概述

DQN将Q-learning与深度神经网络结合，通过神经网络拟合Q函数。在电池管理系统中，DQN主要用于：

- **充电档位选择**：离散充电策略选择
- **档位切换**：充放电模式切换
- **简单控制**：离散控制问题

## 核心公式

**Q-Learning更新**：
```
Q(s,a) ← Q(s,a) + α[r + γ·max_{a'} Q(s',a') - Q(s,a)]
```

**DQN创新**：
- 用神经网络拟合Q(s,a; θ)
- 经验回放（Experience Replay）
- 目标网络（Target Network）

**损失函数**：
```
L(θ) = E[(r + γ·max_{a'} Q(s',a'; θ⁻) - Q(s,a; θ))²]
```

## 代码示例

```python
import torch
import torch.nn as nn
import torch.optim as optim
import random
from collections import deque

class DQN(nn.Module):
    """
    DQN网络
    """
    def __init__(self, state_dim, action_dim):
        super().__init__()

        self.network = nn.Sequential(
            nn.Linear(state_dim, 128),
            nn.ReLU(),
            nn.Linear(128, 128),
            nn.ReLU(),
            nn.Linear(128, action_dim)
        )

    def forward(self, x):
        return self.network(x)

class DQNAgent:
    """
    DQN智能体
    """
    def __init__(self, state_dim, action_dim, lr=0.001):
        self.state_dim = state_dim
        self.action_dim = action_dim

        # Q网络
        self.q_network = DQN(state_dim, action_dim)
        self.target_network = DQN(state_dim, action_dim)
        self.target_network.load_state_dict(self.q_network.state_dict())

        self.optimizer = optim.Adam(self.q_network.parameters(), lr=lr)

        # 经验回放
        self.replay_buffer = deque(maxlen=10000)

        # 超参数
        self.gamma = 0.99  # 折扣因子
        self.epsilon = 1.0  # ε-贪婪
        self.epsilon_decay = 0.995
        self.epsilon_min = 0.01
        self.batch_size = 64
        self.update_target_freq = 10

    def select_action(self, state):
        """
        选择动作（ε-贪婪）
        """
        if random.random() < self.epsilon:
            return random.randint(0, self.action_dim - 1)
        else:
            with torch.no_grad():
                state = torch.FloatTensor(state).unsqueeze(0)
                q_values = self.q_network(state)
                return q_values.argmax().item()

    def store_transition(self, state, action, reward, next_state, done):
        """
        存储经验
        """
        self.replay_buffer.append((state, action, reward, next_state, done))

    def train(self, step):
        """
        训练
        """
        if len(self.replay_buffer) < self.batch_size:
            return

        # 采样batch
        batch = random.sample(self.replay_buffer, self.batch_size)
        states, actions, rewards, next_states, dones = zip(*batch)

        states = torch.FloatTensor(states)
        actions = torch.LongTensor(actions)
        rewards = torch.FloatTensor(rewards)
        next_states = torch.FloatTensor(next_states)
        dones = torch.FloatTensor(dones)

        # 计算Q值
        current_q_values = self.q_network(states).gather(1, actions.unsqueeze(1))

        with torch.no_grad():
            next_q_values = self.target_network(next_states).max(1)[0]
            target_q_values = rewards + (1 - dones) * self.gamma * next_q_values

        # 损失
        loss = nn.MSELoss()(current_q_values.squeeze(), target_q_values)

        # 优化
        self.optimizer.zero_grad()
        loss.backward()
        self.optimizer.step()

        # 更新目标网络
        if step % self.update_target_freq == 0:
            self.target_network.load_state_dict(self.q_network.state_dict())

        # 衰减ε
        self.epsilon = max(self.epsilon_min, self.epsilon * self.epsilon_decay)

        return loss.item()

# 应用：充电档位选择
def charging_gear_selection():
    """
    充电档位选择（离散动作空间）

    状态：[SOC, 温度, 电压, 电流]
    动作：[0.5C, 1C, 1.5C, 2C]（4个档位）
    """
    state_dim = 4  # SOC, T, V, I
    action_dim = 4  # 4个充电档位

    agent = DQNAgent(state_dim, action_dim)

    # 训练循环
    for episode in range(1000):
        state = env.reset()
        done = False

        while not done:
            # 选择动作
            action = agent.select_action(state)

            # 执行动作
            next_state, reward, done = env.step(action)

            # 存储经验
            agent.store_transition(state, action, reward, next_state, done)

            # 训练
            loss = agent.train(step)

            state = next_state

    return agent
```

## 应用场景

- **充电档位选择**：0.5C/1C/1.5C/2C
- **模式切换**：充电/放电/静置
- **简单控制**：离散控制问题

## 电池类型适用性

✅ **100%通用**

## 部署建议

- **云端训练**：大规模训练
- **边缘推理**：轻量级推理
- **Double DQN**：解决过估计问题

## 参考文献

1. Mnih, V., et al. (2015). "Human-level control through deep reinforcement learning". *Nature*.
2. Stable-Baselines3 documentation: https://stable-baselines3.readthedocs.io/
