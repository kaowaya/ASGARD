# B2.5 SOC估计-UKF

> **版本**: V1.0
> **日期**: 2026-03-08
> **Skill ID**: B2.5
> **所属层级**: L2 BMS层级
> **算法类型**: 状态估计 - 无迹卡尔曼滤波

---

## 基本信息

| 属性 | 值 |
|-----|---|
| **算法** | 无迹卡尔曼滤波 |
| **适用性** | ✅ 100%通用 |
| **精度** | ±1% |
| **算力需求** | <50MHz |
| **部署位置** | BMS板端 |

---

## 算法原理

### UT变换

```python
def unscented_transform(x, P, alpha=1e-3, beta=2, kappa=0):
    """生成Sigma点"""
    n = len(x)
    lambda_ = alpha**2 * (n + kappa) - n

    # Sigma点
    sigma_points = np.zeros((2*n+1, n))
    sigma_points[0] = x

    U = np.linalg.cholesky((n + lambda_) * P)
    for i in range(n):
        sigma_points[i+1] = x + U[i]
        sigma_points[n+i+1] = x - U[i]

    # 权重
    Wm = np.zeros(2*n+1)
    Wc = np.zeros(2*n+1)
    Wm[0] = lambda_ / (n + lambda_)
    Wc[0] = lambda_ / (n + lambda_) + (1 - alpha**2 + beta)
    for i in range(1, 2*n+1):
        Wm[i] = Wc[i] = 1 / (2 * (n + lambda_))

    return sigma_points, Wm, Wc
```

---

## 性能指标

| 指标 | 目标值 |
|-----|--------|
| **SOC估计精度** | ±1% |
| **计算负载** | <50MHz |

---

## 关键技术亮点

1. **最高精度**: 强非线性场景下精度达±1%
2. **无需线性化**: 避免EKF的雅可比矩阵计算
3. **稳定性好**: Sigma点策略保证正定性
4. **100%通用**: 适用于所有电池类型

---

**文档作者**: ASGARD产品设计团队
**更新日期**: 2026-03-08
**下次评审**: 2026-04-15
